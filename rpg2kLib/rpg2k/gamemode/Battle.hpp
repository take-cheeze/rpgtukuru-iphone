#ifndef _INC__RPG2k__GAMEMODE_BATTLE_HPP
#define _INC__RPG2k__GAMEMODE_BATTLE_HPP

// #include <GameMode.hpp>


namespace rpg2k
{
	namespace gamemode
	{
		PP_defineGameMode(Battle);
/*
		class Battle : public GameMode
		{
		private:
		public:
			Battle(rpg2k::Main& m);

			void reset();
			void run(input::KeyListener& keyList);
			void gameModeChanged();
		}; // class Battle
 */
	} // namespace gamemode
} // namespace rpg2k

#endif
